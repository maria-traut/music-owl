import { StyledFooter, StyledFooterText } from "./Footer.styled";

export default function Footer() {
  return (
    <StyledFooter>
      <StyledFooterText>
        Inspired by the documentary <span>Alive Inside.</span>
      </StyledFooterText>
    </StyledFooter>
  );
}
